<form method="post" action="{{ path('app_salledesport_delete', {'idSalle': salledesport.idSalle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salledesport.idSalle) }}">
    <button class="btn btn-link">Delete</button>
</form>
