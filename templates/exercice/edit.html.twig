{% extends 'base-back.html.twig' %}

{% block title %}Edit Exercice{% endblock %}

{% block body %}
    <div class="row mb-3">
        <div class="col-2">
            <div class="card mt-3 text-center shadow-lg"><a class=" btn btn" href="{{ path('app_exercice_index') }}"><i class="mdi mdi-arrow-left-bold"></i></a></div>

        </div>
        <div class="col-10">
            <h1 class="text-center m-2">Edit Exercice</h1>
        </div>
    </div>

    {{ include('exercice/_form.html.twig', {'button_label': 'Update'}) }}


{% endblock %}
