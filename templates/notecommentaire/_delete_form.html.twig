<form method="post" action="{{ path('app_notecommentaire_delete', {'idnote': notecommentaire.idnote}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notecommentaire.idnote) }}">
    <button class="btn">Delete</button>
</form>
