<form method="post" action="{{ path('app_wishlist_delete', {'idwishlist': wishlist.idwishlist}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wishlist.idwishlist) }}">
    <button class="btn">Delete</button>
</form>
