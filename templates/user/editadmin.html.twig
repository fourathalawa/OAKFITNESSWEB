{% extends 'base-front.html.twig' %}

{% block title %}Edit User{% endblock %}
{% block session %}{% if(user.iduser !=0) %}
{% elseif(user.iduser==3) %}
    <li class=""><a href="{{ path('app_user_alladmin') }}">Dashboard</a>

    </li>
{% endif %}

{% endblock %}
{% block body %}
    <h1>Edit User</h1>

    {{ include('user/formadmin.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_show') }}">Back</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
