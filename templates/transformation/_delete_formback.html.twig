<form method="post" action="{{ path('app_transformation_deleteback', {'idimage': transformation.idimage}) }}" onsubmit="return confirm('Admin !Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transformation.idimage) }}">
    <button class="btn">Delete</button>
</form>
