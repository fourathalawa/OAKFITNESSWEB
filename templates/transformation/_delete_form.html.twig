<form method="post" action="{{ path('app_transformation_delete', {'idimage': transformation.idimage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transformation.idimage) }}">
    <button class="btn">Delete</button>
</form>
